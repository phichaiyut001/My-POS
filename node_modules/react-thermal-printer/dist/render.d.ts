import { ComponentProps, ReactElement } from 'react';
import { Printer } from './components/Printer';
declare type PrinterProps = ComponentProps<typeof Printer>;
/**
 * Render the React element as printable binary data.
 */
export declare function render(elem: ReactElement<PrinterProps>): Promise<Uint8Array>;
export {};
