import { TextSize } from '@react-thermal-printer/printer';
/** wrap text to multiple lines */
export declare function wrapText(text: string, options: {
    size?: TextSize;
    width: number;
}): string[];
