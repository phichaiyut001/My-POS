import { Printable } from '../types/Printable';
interface Props {
    /**
     * cut after line feeds
     * @default 6
     */
    lineFeeds?: number;
}
export declare const Cut: Printable<Props>;
export {};
