import { Align, TextFont, TextSize, TextUnderline } from '@react-thermal-printer/printer';
import { ReactNode } from 'react';
import { ExtendHTMLProps } from '../types/HTMLProps';
import { Printable } from '../types/Printable';
declare type Props = ExtendHTMLProps<'div', {
    align?: Align;
    bold?: boolean;
    font?: TextFont;
    underline?: TextUnderline;
    invert?: boolean;
    size?: {
        width: TextSize;
        height: TextSize;
    };
    /** if true, don't feed line after print text */
    inline?: boolean;
    children?: ReactNode;
}>;
export declare const Text: Printable<Props>;
export {};
